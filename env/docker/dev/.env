# Monitoring tools
PROMETHEUS_DATASOURCE_UID=prometheus_dev
PROMETHEUS_PORT=7090
GRAFANA_PORT=7000

# Distributed Tracing (services run on host, so use localhost)
ZIPKIN_EXTERNAL_PORT=7411
ZIPKIN_BASE_URL=http://localhost:7411
