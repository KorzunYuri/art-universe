# Production environment config for Music Quiz module
# Database runs on Windows host, service runs in Docker
# Service connects to database via host.docker.internal

# Application
MU_QUIZ_APP_INTERNAL_PORT=8080
MU_QUIZ_APP_CORS_ALLOWED_ORIGINS=http://localhost:3000

SPRING_PROFILES_ACTIVE=prod
