# Shared Persistent Volumes - Docker Compose <-> Kubernetes
# Points hostPath PVs to Docker named volume directories in the WSL2 VM.
# IMPORTANT: Stop Docker Compose before starting K8s (and vice versa).
# PostgreSQL requires exclusive access to its data directory.

# --- PostgreSQL: Master ---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-master-data
  labels:
    app: postgres-master
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hostpath
  hostPath:
    path: /var/lib/docker/volumes/local__au__db-postgres-data/_data
    type: DirectoryOrCreate
  claimRef:
    name: data-postgres-master-0
    namespace: mu-data
---
# --- PostgreSQL: Replica ---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-replica-data
  labels:
    app: postgres-replica
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hostpath
  hostPath:
    path: /var/lib/docker/volumes/local__au__db-postgres-replica-data/_data
    type: DirectoryOrCreate
  claimRef:
    name: data-postgres-replica-0
    namespace: mu-data
---
# --- Prometheus ---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-prometheus-data
  labels:
    app: prometheus
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hostpath
  hostPath:
    path: /var/lib/docker/volumes/local__mu__prometheus-data/_data
    type: DirectoryOrCreate
  claimRef:
    name: data-prometheus-0
    namespace: art-universe-monitoring
---
# --- Grafana ---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-grafana-data
  labels:
    app: grafana
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hostpath
  hostPath:
    path: /var/lib/docker/volumes/local__mu__grafana-data/_data
    type: DirectoryOrCreate
  claimRef:
    name: grafana-data
    namespace: art-universe-monitoring
---
# --- Grafana PVC (Deployment needs explicit PVC, unlike StatefulSets) ---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: art-universe-monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hostpath
  resources:
    requests:
      storage: 1Gi
