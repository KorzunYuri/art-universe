apiVersion: v1
kind: Service
metadata:
  name: lastfm-etl-rest-api
  namespace: mu-lastfm
  labels:
    app: lastfm-etl-rest-api
    app.kubernetes.io/name: lastfm-etl-rest-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: lastfm-etl-rest-api
