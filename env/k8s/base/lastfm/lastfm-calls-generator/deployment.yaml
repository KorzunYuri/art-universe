apiVersion: apps/v1
kind: Deployment
metadata:
  name: lastfm-calls-generator
  namespace: mu-lastfm
  labels:
    app: lastfm-calls-generator
    app.kubernetes.io/name: lastfm-calls-generator
spec:
  replicas: 1
  # Not scalable - use Recreate strategy
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: lastfm-calls-generator
  template:
    metadata:
      labels:
        app: lastfm-calls-generator
        app.kubernetes.io/name: lastfm-calls-generator
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8080"
    spec:
      initContainers:
        - name: wait-for-database
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for database..."
              until nc -z postgres-lastfm-master.mu-data.svc.cluster.local 5432; do
                sleep 2
              done
              echo "Database ready!"
              sleep 10
      containers:
        - name: app
          image: mu-lastfm-calls-generator:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
              name: actuator
          env:
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx768m -XX:MaxMetaspaceSize=192m"
            - name: MURAW_LASTFM_DB_WRITER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: lastfm-db-credentials
                  key: MURAW_LASTFM_DB_WRITER_PASSWORD
            - name: MURAW_LASTFM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: lastfm-api-key
                  key: MURAW_LASTFM_API_KEY
          envFrom:
            - configMapRef:
                name: lastfm-common-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "200m"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            periodSeconds: 5
            failureThreshold: 3
