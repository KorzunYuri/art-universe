apiVersion: v1
kind: Service
metadata:
  name: lastfm-calls-generator
  namespace: mu-lastfm
  labels:
    app: lastfm-calls-generator
spec:
  selector:
    app: lastfm-calls-generator
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: actuator
