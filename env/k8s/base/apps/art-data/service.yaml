apiVersion: v1
kind: Service
metadata:
  name: art-data
  namespace: mu-apps
  labels:
    app: art-data
    app.kubernetes.io/name: art-data
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: art-data
