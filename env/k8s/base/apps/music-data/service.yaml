apiVersion: v1
kind: Service
metadata:
  name: music-data
  namespace: mu-apps
  labels:
    app: music-data
    app.kubernetes.io/name: music-data
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: music-data
