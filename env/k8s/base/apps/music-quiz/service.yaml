apiVersion: v1
kind: Service
metadata:
  name: music-quiz
  namespace: mu-apps
  labels:
    app: music-quiz
    app.kubernetes.io/name: music-quiz
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: music-quiz
