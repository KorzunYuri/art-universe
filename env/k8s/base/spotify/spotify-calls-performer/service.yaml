apiVersion: v1
kind: Service
metadata:
  name: spotify-calls-performer
  namespace: mu-spotify
  labels:
    app: spotify-calls-performer
spec:
  selector:
    app: spotify-calls-performer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: actuator
