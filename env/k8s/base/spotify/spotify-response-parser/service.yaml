apiVersion: v1
kind: Service
metadata:
  name: spotify-response-parser
  namespace: mu-spotify
  labels:
    app: spotify-response-parser
spec:
  selector:
    app: spotify-response-parser
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: actuator
