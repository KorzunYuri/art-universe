apiVersion: v1
kind: Service
metadata:
  name: spotify-rest-api
  namespace: mu-spotify
  labels:
    app: spotify-rest-api
    app.kubernetes.io/name: spotify-rest-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: spotify-rest-api
