apiVersion: v1
kind: Service
metadata:
  name: zipkin
  namespace: art-universe-monitoring
  labels:
    app: zipkin
    app.kubernetes.io/name: zipkin
spec:
  type: ClusterIP
  ports:
    - port: 9411
      targetPort: 9411
      name: http
  selector:
    app: zipkin
