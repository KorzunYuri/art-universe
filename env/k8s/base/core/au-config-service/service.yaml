apiVersion: v1
kind: Service
metadata:
  name: au-config-service
  namespace: au-core
  labels:
    app: au-config-service
    app.kubernetes.io/name: au-config-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: au-config-service
