apiVersion: v1
kind: Service
metadata:
  name: au-auth-service
  namespace: au-core
  labels:
    app: au-auth-service
    app.kubernetes.io/name: au-auth-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: au-auth-service
