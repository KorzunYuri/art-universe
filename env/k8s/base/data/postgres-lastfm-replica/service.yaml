apiVersion: v1
kind: Service
metadata:
  name: postgres-lastfm-replica
  namespace: mu-data
  labels:
    app: postgres-lastfm-replica
    app.kubernetes.io/name: postgres-lastfm-replica
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for StatefulSet
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgres-lastfm-replica
