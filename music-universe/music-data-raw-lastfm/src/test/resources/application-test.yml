spring:
  jpa:
    properties:
      hibernate:
        show-sql: true
        jdbc:
          batch_size: 1
  docker:
    compose:
      enabled: false

lastfm:
  apikey: DUMMY_API_KEY

logging:
  level:
    org:
      springframework:
        transaction: TRACE
      hibernate:
        transaction: DEBUG
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
        orm:
          jdbc:
            bind: TRACE
