plugins {
    id 'art-universe.spring-boot-web-app'
}

archivesBaseName = 'lastfm-rest-api'

dependencies {
    implementation project(':music:data:raw:lastfm:lastfm-repositories')

    testImplementation project(':music:data:raw:lastfm:test:lastfm-test-database-helper')
}