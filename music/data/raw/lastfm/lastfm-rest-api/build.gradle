plugins {
    id 'art-universe.spring-boot-web-app'
}

archivesBaseName = 'lastfm-rest-api'

dependencies {
    implementation project(':music:data:raw:lastfm:lastfm-models')

    testImplementation project(':music:data:raw:lastfm:test:lastfm-test-with-database')
}