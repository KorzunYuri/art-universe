{
  "tracks": {
    "track": [
      {
        "name": "string — track name",
        "duration": "string — duration in seconds (numeric string)",
        "mbid": "string — MusicBrainz ID (may be empty)",
        "url": "string — Last.fm track URL",
        "streamable": {
          "#text": "string — '0' or '1'",
          "fulltrack": "string — '0' or '1'"
        },
        "artist": {
          "name": "string — artist name",
          "mbid": "string — MusicBrainz ID (may be empty)",
          "url": "string — Last.fm artist URL"
        },
        "image": ["...same image structure..."],
        "@attr": {
          "rank": "string — 1-based rank (numeric string)"
        }
      }
    ],
    "@attr": {
      "tag": "string — tag name",
      "page": "string",
      "perPage": "string",
      "totalPages": "string",
      "total": "string"
    }
  }
}