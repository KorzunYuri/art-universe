spring:
  jpa:
    properties:
      hibernate:
        show-sql: true
        jdbc:
          batch_size: 1
  docker:
    compose:
      enabled: false
  task:
    execution:
      pool:
        core-size: 0
        max-size: 1
  liquibase:
    change-log: classpath:db/migration/muraw/liquibase/db.changelog.xml

lastfm:
  apikey: DUMMY_API_KEY

logging:
  level:
    org:
      springframework:
        transaction: TRACE
      hibernate:
        transaction: DEBUG
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
        orm:
          jdbc:
            bind: TRACE
